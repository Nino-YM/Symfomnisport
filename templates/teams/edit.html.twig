{% extends 'base.html.twig' %}

{% block title %}Edit Teams{% endblock %}

{% block body %}
    <h1>Edit Teams</h1>

    {{ include('teams/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_sections_index') }}" class="btn btn-secondary mt-2">back to list</a>

    {{ include('teams/_delete_form.html.twig', { 'class': 'btn btn-danger mt-2' }) }}
{% endblock %}
