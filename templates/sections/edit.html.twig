{% extends 'base.html.twig' %}

{% block title %}Edit Sections{% endblock %}

{% block body %}
    <h1 class="my-4">Edit Sections</h1>

    {{ include('sections/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_sections_index') }}" class="btn btn-secondary mt-2">back to list</a>

    {{ include('sections/_delete_form.html.twig', { 'class': 'btn btn-danger mt-2' }) }}
{% endblock %}
