{% extends 'base.html.twig' %}

{% block title %}Edit Sports{% endblock %}

{% block body %}
    <h1 class="my-4">Edit Sports</h1>

    {{ include('sections/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_sections_index') }}" class="btn btn-secondary mt-2">Back to list</a>

    {{ include('sections/_delete_form.html.twig', { 'id': section.id, 'button_class': 'btn btn-danger mt-2', 'button_label': 'Delete' }) }}
{% endblock %}
