{% extends 'base.html.twig' %}

{% block title %}Sections index{% endblock %}

{% block body %}
    <h1 class="my-4">Sections Index</h1>
    {% if is_granted('ROLE_ADMIN') %}
        <div class="mb-3">
            <a href="{{ path('app_sections_new') }}" class="btn btn-primary">Create New Section</a>
        </div>
    {% endif %}
    <table class="table table-striped">
        <thead class="table-dark">
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for section in sections %}
            <tr>
                <td>{{ section.name }}</td>
                <td>
                    <a href="{{ path('app_sections_show', {'id': section.id}) }}" class="btn btn-info btn-sm">Show</a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_sections_edit', {'id': section.id}) }}" class="btn btn-warning btn-sm">Edit</a>
                    {% endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2" class="text-center">No records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
