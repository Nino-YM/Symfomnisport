{% extends 'base.html.twig' %}

{% block title %}Activity Details{% endblock %}

{% block body %}
    <h1 class="my-4">Activity Details</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th scope="row">Name</th>
                <td>{{ activity.name }}</td>
            </tr>
            <tr>
                <th scope="row">Description</th>
                <td>{{ activity.description }}</td>
            </tr>
            <tr>
                <th scope="row">Date</th>
                <td>{{ activity.date ? activity.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_activities_index') }}" class="btn btn-info mr-2">Back to list</a>

    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_activities_edit', {'id': activity.id}) }}" class="btn btn-warning mr-2">Edit</a>
        {{ include('activities/_delete_form.html.twig', { 'class': 'btn btn-danger' }) }}
    {% endif %}
{% endblock %}
