{% extends 'base.html.twig' %}

{% block title %}Edit Members{% endblock %}

{% block body %}
    <h1 class="my-4">Edit Members</h1>

    {{ include('members/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_members_index') }}" class="btn btn-secondary mt-2">back to list</a>

    {{ include('members/_delete_form.html.twig', { 'class': 'btn btn-danger mt-2' }) }}
{% endblock %}
